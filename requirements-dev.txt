# Development and test dependencies
# Not installed in production Docker image

pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.27.0  # already in requirements.txt but listed here for clarity; pip dedupes

# Linting (used in CI lint job)
ruff>=0.4.0
